import LayoutApp from "../../screens/LayoutApp";
import MachineList from "./MachineList";

const Machine = () => {
  return (
     <LayoutApp Pages={<MachineList />} />
  );
};

export default Machine;
